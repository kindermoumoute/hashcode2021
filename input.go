package main

type RawInput struct {
	Raw      string
	FileName string
}

type Input struct {

	// Usefull data

}

func DecodeInput(s string) Input {
	input := Input{}

	//lines := strings.Split(s, "\n")
	//header := pkg.ParseIntList(lines[0], " ")
	//for _, headerValue := range header[1:] {
	//
	//}

	//for id, line := range lines[1:] {
	//	if len(line) == 0 { // trailing line
	//		break
	//	}
	//
	//}

	return input
}
